{
  "name": "backend",
  "version": "1.0.0",
  "description": "db json",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "anna rafaela",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
